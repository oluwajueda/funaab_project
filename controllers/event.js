// const Event = require("../models/testimony");

// const createEvent = async (req, res) => {
//   req.body.createdBy = req.user.userId;
//   const event = await Event.create(req.body);
//   res.status(200).json({ event });
// };

// module.exports = {
//   createEvent,
// };
